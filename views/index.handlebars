<div class="container">
<h1>BURGER GRAVEYARD</h1>
</div>

<div class="burger-list">
<div class="devoured-burgers">
<h2>You ate these burgers.</h2>

<ul>
    {{#each burgers}}
      {{#unless devoured}}
        {{> burgers/burger-block devoured=true}}
      {{/unless}}
    {{/each}}
</ul>
</div>

<div class="not-devoured">
<h2>You want you should order somthing?</h2>

<ul>
    {{#each burgers}}
      {{#if devoured}}
        {{> burgers/burger-block devoured=false}}
      {{/if}}
    {{/each}}
</ul>
</div>
</div>


<div class="container">
  <h2>Add a Burger:</h2>
  <h4>Enter a burger you'd like to eat, and click submit</h4>
  <form class="create-form">
    
    <p>Burger Name:</p>
    <div class="form-group">
      <textarea type="text" id="burger" rows="4" name="name" placeholder="Bacon Cheeseburger"></textarea>
    </div>
    <button type="submit" class="form-button">Add Burger</button>
  
</div>